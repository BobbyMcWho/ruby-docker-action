name: 'Ruby Docker'
description: 'Execute a command in a ruby container.'
inputs:
  ruby-version:
    description: 'Docker tag of target ruby version from '
    required: true
    default: 'alpine'
runs:
  using: 'docker'
  image: 'docker://ruby:2.6.3'
  entrypoint: ruby
  args:
    - -v
